const verticalAlign = "bottom";
const horizontalAlign = "center";
const iconError = "ti-thumb-down";
const iconSuccess = "ti-thumb-up";
const iconInfo = "ti-info";
const iconNetwork = "ti-power-off";

export const opts = {
  verticalAlign,
  horizontalAlign,
  iconError,
  iconSuccess,
  iconInfo,
  iconNetwork
};
